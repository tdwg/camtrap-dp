{
  "fields": [
    {
      "name": "deployment_id",
      "type": "string",
      "format": "default",
      "title": "Deployment ID",
      "description": "A unique identifier of a deployment within a project.",
      "example": "L01-D01",
      "constraints": {
        "required": true,
        "unique": true
      }
    },
    {
      "name": "location_id",
      "type": "string",
      "format": "default",
      "description": "A unique identifier of a sampling location within a project.",
      "example": "L01",
      "constraints": {
        "required": false
      }
    },
    {
      "name": "location_name",
      "type": "string",
      "format": "default",
      "description": "Name given to the sampling location.",
      "example": "Białowieża MRI 01",
      "constraints": {
        "required": false
      }
    },
    {
      "name": "longitude",
      "type": "number",
      "format": "default",
      "description": "Longitude of the sampling location in decimal degrees, using the WGS84 datum.",
      "example": "23.84995",
      "constraints": {
        "required": true,
        "minimum": -180,
        "maximum": 180
      }
    },
    {
      "name": "latitude",
      "type": "number",
      "format": "default",
      "description": "Latitude of the sampling location in decimal degrees, using the WGS84 datum.",
      "example": "52.70442",
      "constraints": {
        "required": true,
        "minimum": -90,
        "maximum": 90
      }
    },
    {
      "name": "start",
      "type": "datetime",
      "format": "any",
      "description": "Date and time when the deployment started, as an ISO 8601 formatted string in UTC (e.g. `YYYY-MM-DDThh:mm:ssZ`).",
      "example": "2020-03-24T11:21:46Z",
      "constraints": {
        "required": true
      }
    },
    {
      "name": "end",
      "type": "datetime",
      "format": "any",
      "description": "Date and time when the deployment ended, as an ISO 8601 formatted string in UTC (e.g. `YYYY-MM-DDThh:mm:ssZ`).",
      "example": "2020-03-24T11:21:46Z",
      "constraints": {
        "required": true
      }
    },
    {
      "name": "setup_by",
      "type": "string",
      "format": "default",
      "description": "Name or a unique identifier of the person who set up the camera for this deployment.",
      "example": [
        "Charles Robert Darwin",
        "2ef60d48-fd67-4bac-9569-49a03b0ef096"
      ],
      "constraints": {
        "required": false
      }
    },
    {
      "name": "cam_id",
      "type": "string",
      "format": "default",
      "description": "A unique identifier of a camera amongst all cameras used in a project. This can be e.g. a camera device serial number.",
      "example": "P800HG08192031",
      "constraints": {
        "required": false
      }
    },
    {
      "name": "cam_model",
      "type": "string",
      "format": "default",
      "description": "Manufacturer and model of camera provided in this format: `manufacturer-model`.",
      "example": "Reconyx-PC800",
      "constraints": {
        "required": false
      }
    },
    {
      "name": "cam_timestamp",
      "type": "boolean",
      "description": "A flag indicating if a datetime settings in a camera trap were set up properly and in consequence all recorded files have proper timestamps.",
      "example": "true",
      "constraints": {
        "required": false
      }
    },
    {
      "name": "cam_interval",
      "type": "integer",
      "description": "Time specified between shutter triggers when activity in the sensor will not trigger the shutter. Specified in seconds.",
      "example": "0",
      "constraints": {
        "required": false,
        "minimum": 0
      }
    },
    {
      "name": "cam_height",
      "type": "integer",
      "description": "An approximate height at which a camera was deployed. Specified in centimeters.",
      "example": "100",
      "constraints": {
        "required": false,
        "minimum": 0
      }
    },
    {
      "name": "cam_angle",
      "type": "integer",
      "format": "default",
      "description": "An approximate angle used to deploy a camera. Specified in degrees e.g. 0 or 180 for a camera deployed in parallel to the ground and 90 for a perpendicular deployment.",
      "example": "180",
      "constraints": {
        "required": false,
        "minimum": 0,
        "maximum": 180
      }
    },
    {
      "name": "bait_use",
      "type": "string",
      "format": "default",
      "description": "Type of bait (if any) that was used with camera. If other, more info can be provided in the `comments` field.",
      "example": "",
      "constraints": {
        "required": false,
        "enum": [
          "None",
          "Scent",
          "Food",
          "Visual",
          "Acoustic",
          "Other"
        ]
      }
    },
    {
      "name": "session",
      "type": "string",
      "format": "default",
      "description": "Camera trap deployments can be grouped into sessions. Common sessions could be seasons (wet and dry), months, years or other types of logical groupings when field sampling occurs.",
      "example": "",
      "constraints": {
        "required": false
      }
    },
    {
      "name": "array",
      "type": "string",
      "format": "default",
      "description": "A name for a logical grouping of deployments into (spatial) arrays. This could be for thematic or logistical reasons.",
      "example": "",
      "constraints": {
        "required": false
      }
    },
    {
      "name": "feature_type",
      "type": "string",
      "format": "default",
      "description": "Type of feature (if any) that camera deployment is associated with. If other, more info can be provided in the `comments` field.",
      "example": "",
      "constraints": {
        "required": false,
        "enum": [
          "None",
          "Road paved",
          "Road dirt",
          "Trail hiking",
          "Trail game",
          "Road underpass",
          "Road overpass",
          "Road bridge",
          "Culvert",
          "Burrow",
          "Nest site",
          "Carcass",
          "Water source",
          "Fruiting tree",
          "Other"
        ]
      }
    },
    {
      "name": "habitat",
      "type": "string",
      "format": "default",
      "description": "Short characterization of the habitat.",
      "example": "Mixed temperate low-land forest",
      "constraints": {
        "required": false
      }
    },
    {
      "name": "tags",
      "type": "array",
      "format": "default",
      "description": "User defined tags associated with the deployment, as a valid JSON array.",
      "example": "[ \"Control\", \"Treatment\" ]",
      "constraints": {
        "required": false
      }
    },
    {
      "name": "comments",
      "type": "string",
      "format": "default",
      "constraints": {
        "required": false,
        "pattern": "[^,;]"
      }
    },
    {
      "name": "_id",
      "type": "integer",
      "description": "Internal attribute of data management system: ID of this deployment.",
      "constraints": {
        "required": false
      }
    }
  ],
  "missingValues": [
    "",
    "NaN",
    "nan"
  ]
}
